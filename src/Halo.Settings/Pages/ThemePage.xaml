<UserControl x:Class="Halo.Settings.Pages.ThemePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="18,14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0">
      <TextBlock Text="Theme" Style="{StaticResource PageTitle}"/>
      <TextBlock Style="{StaticResource PageSub}"
                 Text="Colors used by every widget (saved to config\theme.json). Values are R G B A (0–255); the defaults are the extracted Rainformer palette. Widgets recolor live on Save."/>
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
      <TextBlock Text="Scale override" Margin="0,0,10,0"/>
      <TextBox x:Name="ScaleBox" Width="90"/>
      <TextBlock Text="Same value as 'Widget scale' on the General page (both edit theme.json)."
                 Foreground="{StaticResource Sub}" Margin="16,0,0,0"/>
    </StackPanel>

    <DataGrid Grid.Row="2" x:Name="ColorGrid" CanUserAddRows="False">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Token" Binding="{Binding Token}" IsReadOnly="True" Width="150"/>
        <DataGridTextColumn Header="Used for" Binding="{Binding Desc}" IsReadOnly="True" Width="210"/>
        <DataGridTextColumn Header="R" Binding="{Binding R}" Width="55"/>
        <DataGridTextColumn Header="G" Binding="{Binding G}" Width="55"/>
        <DataGridTextColumn Header="B" Binding="{Binding B}" Width="55"/>
        <DataGridTextColumn Header="A" Binding="{Binding A}" Width="55"/>
        <DataGridTemplateColumn Header="Preview" Width="130">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <Border Background="{Binding Swatch}" Height="18" Width="104" Margin="6,2"
                      BorderBrush="#606060" BorderThickness="1" CornerRadius="2"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>

    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
      <Button Content="Save" Click="Save_Click"/>
      <TextBlock x:Name="Status" Foreground="{StaticResource Sub}" VerticalAlignment="Center" TextWrapping="Wrap"/>
    </StackPanel>
  </Grid>
</UserControl>
