<UserControl x:Class="Halo.Settings.Pages.ThemePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="18,14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Theme (config\theme.json override)" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
      <TextBlock Text="Scale" Margin="0,0,10,0"/>
      <TextBox x:Name="ScaleBox" Width="90"/>
      <TextBlock Text="Empty tokens fall back to the extracted Rainformer defaults."
                 Foreground="{StaticResource Sub}" Margin="16,0,0,0"/>
    </StackPanel>

    <DataGrid Grid.Row="2" x:Name="ColorGrid" CanUserAddRows="False">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Token" Binding="{Binding Token}" IsReadOnly="True" Width="180"/>
        <DataGridTextColumn Header="R" Binding="{Binding R}" Width="60"/>
        <DataGridTextColumn Header="G" Binding="{Binding G}" Width="60"/>
        <DataGridTextColumn Header="B" Binding="{Binding B}" Width="60"/>
        <DataGridTextColumn Header="A" Binding="{Binding A}" Width="60"/>
        <DataGridTemplateColumn Header="Preview" Width="140">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <Border Background="{Binding Swatch}" Height="18" Width="110" Margin="6,2"
                      BorderBrush="#606060" BorderThickness="1" CornerRadius="2"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>

    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
      <Button Content="Save" Click="Save_Click"/>
      <TextBlock x:Name="Status" Foreground="{StaticResource Sub}" VerticalAlignment="Center" TextWrapping="Wrap"/>
    </StackPanel>
  </Grid>
</UserControl>
